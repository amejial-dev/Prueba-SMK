PORT=3000

DB_HOST=db
DB_PORT=5432
DB_NAME=prueba_smk
DB_USER=postgres
DB_PASSWORD=postgres

JWT_SECRET=cambia-este-secreto
JWT_EXPIRES_IN=1d

# Solo se usan al ejecutar `npm run seed:admin` (no las lee el servidor Express)
ADMIN_SEED_NOMBRE=admin
ADMIN_SEED_PASSWORD=cambia-esta-clave
